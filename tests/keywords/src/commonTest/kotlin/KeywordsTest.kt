import keywords.*
import kotlin.test.Test

class KeywordsTest {

    @Test
    fun test() {
        // able to import is the real test, but might as well call something.
        `if`(1.toUByte())
    }
}